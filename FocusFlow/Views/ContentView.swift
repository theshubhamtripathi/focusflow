// ContentView.swift
// Temporary placeholder. We'll build the real TabView on Day 2.

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("FocusFlow 🍅")
            .font(.largeTitle)
            .fontWeight(.bold)
    }
}

#Preview {
    ContentView()
}
